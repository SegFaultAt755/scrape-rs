[package]
name = "test"
version = "0.1.0"
edition = "2024"

[dependencies]
ureq = { version = "3.4.0", features = ["json"] }
serde = { version = "1.0", features = ["derive"] }
scrape-rs = { path = "../scrape-rs" }

